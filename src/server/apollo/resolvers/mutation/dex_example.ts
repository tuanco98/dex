/**
 * Example
 * @param parent
 * @param {InputType} args
 * @param ctx
 * @return {OutputType}
 */

export const rls_login = async (_, __, ___) => {
	// rome-ignore lint/correctness/noUnusedVariables: <explanation>
	const now = +new Date();
	try {
		// rome-ignore lint/correctness/noUnreachable: <explanation>
	} catch (e) {
		throw e;
	}
};
