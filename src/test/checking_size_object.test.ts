import { connectInfra } from "../infra";

const test = async () => {
    connectInfra()
    // let object_size_observer = new ObjectSizeObserver()
    // let a: any[] = []
    // object_size_observer.add(a, "a")
    // while (true) {
    //     a.push(1)
    //     object_size_observer.notify()
    //     await sleep(1000)
    // }
}

test()